sudo: false
language: cpp
compiler:
  - gcc
  - clang
install: if [ "$CXX" = "g++" ]; then export CXX="g++-5"; fi
script:
  - make
  - make test
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-5
      - g++-5
